apiVersion: v1
kind: Service

metadata:
  name: zookeeper
  namespace: {{ .Values.namespace }}

spec:
  clusterIP: None

  selector:
    app: zookeeper

  ports:
    - port: 2181
      targetPort: 2181
