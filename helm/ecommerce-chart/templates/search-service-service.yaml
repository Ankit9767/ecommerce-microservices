apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: {{ .Values.namespace }}

spec:
  selector:
    app: search-service

  ports:
    - port: 8079
      targetPort: 8079

  type: ClusterIP