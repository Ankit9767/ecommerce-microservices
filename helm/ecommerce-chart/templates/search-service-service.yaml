apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: {{ .Values.namespace }}

spec:
  selector:
    app: search-service

  ports:
    - port: 8080
      targetPort: 8080

  type: ClusterIP