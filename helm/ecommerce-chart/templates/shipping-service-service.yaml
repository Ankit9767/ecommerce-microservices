apiVersion: v1
kind: Service

metadata:
  name: shipping-service
  namespace: {{ .Values.namespace }}

spec:

  selector:
    app: shipping-service

  ports:

    - port: 8081

      targetPort: 8081

  type: ClusterIP
