apiVersion: policy/v1
kind: PodDisruptionBudget

metadata:
  name: payment-service-pdb
  namespace: {{ .Values.namespace }}

spec:
  minAvailable: 1

  selector:
    matchLabels:
      app: payment-service