apiVersion: policy/v1
kind: PodDisruptionBudget

metadata:
  name: cart-service-pdb
  namespace: {{ .Values.namespace }}

spec:
  minAvailable: 1

  selector:
    matchLabels:
      app: cart-service
