server:
  port: 8761
  shutdown: graceful

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

management:
  tracing:
    zipkin:
      tracing:
        endpoint: http://zipkin:9411/api/v2/spans